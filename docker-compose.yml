version: '3.8'

services:
  frontend:
    container_name: circle-vibe-frontend
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "5173:5173"
    networks:
      - circle-vibe-network

networks:
  circle-vibe-network:
    driver: bridge